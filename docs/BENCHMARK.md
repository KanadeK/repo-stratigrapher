# Benchmark

Date: 2026-07-18

Machine: Windows development workstation, Node 24.

Dataset: deterministic `examples/fixture-repo` generated by `npm run fixture`.

Fixture size:

- 25 commits
- 3 renames
- 5 failure records
- Payment module hotspot touched repeatedly

Expected command:

```bash
npm run demo
```

Expected result on the sample dataset: analysis completes within a few seconds and writes `dist-demo/report.md` and `dist-demo/report.json`.
